.env
__pycache__/
*.pyc
user_data/
dist/
build/
*.spec

# Claude Code — keep launch.json (dev convenience), ignore machine-local settings
.claude/settings.local.json

# Serena MCP — tool-local config/cache, not part of the app
.serena/
